# Configuration override for a multi-epoch processing for a comprehensive survey
# notably stack convolution is on

imcoadd:
  convolve: False
  target_seeing:  # auto if it's empty or smaller than max seeing
  image_selection: 'auto'

settings:
  factory_scratch:
